<%= form_for @profile, :url => profiles_path do |f| %>
  <div class="form-group">
    <%= f.label :bio, "Personal Profile：" %><br>
    <%= f.text_area :bio, class: "form-control"%>
  </div>
  <%= f.submit "Create", :class => "btn btn-primary" %>
<% end %>

<%= link_to 'Back to index', topics_path %>