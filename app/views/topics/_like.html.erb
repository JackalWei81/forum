  <% unless @topic.find_my_like(current_user) %>
    <%= link_to fa_icon("thumbs-up"), like_topic_path(@topic), :method => :post, :remote => true, :class => "btn btn-primary" %>
  <% else %>
    <%= link_to fa_icon("thumbs-up")+"收回", dislike_topic_path(@topic), :method => :post, :remote => true, :class => "btn btn-danger" %>
  <% end %><%= @topic.likes_by.size %>